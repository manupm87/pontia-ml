# Las dependencias del proyecto viven en pyproject.toml (fuente única).
# Este fichero existe solo para plataformas que instalan desde requirements.txt
# (p. ej. Streamlit Community Cloud): instala el paquete y sus deps de runtime.
#
# Para desarrollo / reentrenamiento / tests usa los extras:
#   pip install -e ".[train,dev]"
-e .
