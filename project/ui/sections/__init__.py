"""Secciones (páginas) de la aplicación Streamlit.

Cada módulo expone una función `render()` que dibuja una sección. Se llaman
"sections" y no "pages" a propósito: Streamlit trata de forma especial cualquier
carpeta llamada `pages/` (multipágina automática), y aquí queremos controlar la
navegación nosotros desde la barra lateral de `app.py`.
"""
